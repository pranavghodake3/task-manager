docker compose exec backend npm run migrate
docker compose exec backend npm run seed

npx sequelize-cli migration:generate --name add-column-to-users

npx sequelize-cli model:generate --name Status --attributes name:string,color:string,display_Order:integer,isClosed:boolean

npx sequelize-cli model:generate --name Priority --attributes name:string,rank:integer,color:string

npx sequelize-cli migration:generate --name add-columns-to-tasks

npx sequelize-cli seed:generate --name statuses

npx sequelize-cli seed:generate --name priorities

npx sequelize-cli migration:generate --name alter-foreign-key-constraint-refresh_tokens

npx sequelize-cli migration:generate --name add-column-slug-to-roles

npx sequelize-cli seed:generate --name roles-alter

npx sequelize-cli model:generate --name Job_Title --attributes name:string,slug:string

npx sequelize-cli model:generate --name CompanyMember --attributes companyId:integer,userId:integer,roleId:integer,jobTitleId:integer

npx sequelize-cli migration:generate --name alter-columns-in-projectMembers

npx sequelize-cli model:generate --name TaskComment --attributes taskId:integer,companyMemberId:integer,comment:text

npx sequelize-cli model:generate --name TaskAttachment --attributes taskId:integer,fileName:string,fileUrl:text

npx sequelize-cli model:generate --name GlobalRole --attributes name:string,slug:string

npx sequelize-cli seed:generate --name globalRoles

npx sequelize-cli model:generate --name TaskType --attributes name:string,slug:string

npx sequelize-cli seed:generate --name taskTypes

kubectl get pods
kubectl get deployment
kubectl get svc
kubectl get job
kubectl apply -f=secret.yaml -f=postgresql-pvc.yaml -f=postgresql.yaml -f=migrate-job.yaml -f=backend.yaml

# Restart a Deployment
kubectl rollout restart deployment/backend-deployment

# Restart a StatefulSet
kubectl rollout restart statefulset/<statefulset-name>

# Restart a DaemonSet
kubectl rollout restart daemonset/<daemonset-name>

# Restart all deployments in a specific namespace
kubectl rollout restart deployment -n <namespace-name>
